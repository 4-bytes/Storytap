class Book {
  String title;
  String description;
  String genre;
  String cover;
  bool isComplete;
  DateTime lastUpdated;
  DateTime creationDate;


  Book({this.title, this.description, this.genre, this.cover, this.isComplete, this.lastUpdated, this.creationDate});
}

